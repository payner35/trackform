# Copy to /opt/dlp/.env on the droplet. Never commit a real .env.
# Generate the token with: openssl rand -hex 32

DLP_API_TOKEN=replace-with-openssl-rand-hex-32-output
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxx
